import { Book } from './entities/Book';
import { Order } from './entities/Order';
import { Profile } from './entities/Profile';
import { User } from './entities/User';

const entities = [User, Order, Book, Profile];

export default entities;
